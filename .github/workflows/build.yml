name: Build plugin

on:
  push:
    branches: [master]
    tags: ["v*"]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Check backend syntax
        run: python3 -m py_compile main.py

      - name: Assemble plugin zip
        run: |
          mkdir -p out/decky-wrapped/dist
          cp plugin.json package.json main.py README.md LICENSE out/decky-wrapped/
          cp dist/index.js out/decky-wrapped/dist/
          cd out && zip -r decky-wrapped.zip decky-wrapped

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: decky-wrapped
          path: out/decky-wrapped.zip

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: out/decky-wrapped.zip
          generate_release_notes: true
